#ifndef __STRG__
#define __STRG__

#include "ff.h"

bool strg_start();
FIL strg_open(const char * filename, const char * open_type);
bool strg_write(FIL * file, uint8_t * data, uint32_t len);


#endif